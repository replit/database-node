{
  "name": "@replit/database",
  "version": "2.0.2",
  "description": "Client for Repl.it Database",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "esbuild index.ts --log-level=warning --outdir='./.build' --outbase=. --sourcemap --target='node16' --platform='node' --format='cjs'"
  },
  "contributors": [
    {
      "name": "Junhao Zhang",
      "url": "https://github.com/mrlapizgithub/repl.it-db"
    }
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/replit/database-node.git"
  },
  "dependencies": {
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.17.8"
  }
}